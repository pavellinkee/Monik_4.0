# Monik — пример конфигурации.
#
# ВНИМАНИЕ: реальные секреты в этот файл не помещаются никогда.
# Credentials задаются ссылками на environment variables ({ env: "MONIK_..." })
# и разрешаются только Configuration subsystem (17_CONFIGURATION.md §5, §26, §46).
#
# Любой параметр можно переопределить переменной окружения с префиксом MONIK__,
# где путь внутри документа разделяется двойным подчёркиванием. Например:
#   MONIK__SCANNER__LEVEL1__INTERVAL_SECONDS=60
#   MONIK__APPLICATION__ENVIRONMENT=production
# Приоритет: environment override > этот файл > безопасный default.

application:
  environment: development        # development | test | production
  timezone: "Europe/Lisbon"
  shutdown_timeout_seconds: 30

# Сети. Круг всегда замыкается внутри одной сети: межсетевых сделок
# сканер не рассматривает (10_LEVEL_1_SCANNER.md §38). Поэтому базовый
# токен round-trip задаётся здесь, у сети, а не общей настройкой сканера.
# Сеть выключается собственным флагом enabled — тогда она не участвует в
# сканировании вовсе (02_LEVEL1_SCANNER.md §72).
networks:
  - network_id: polygon
    name: Polygon
    chain_id: 137
    native_token_symbol: POL
    native_token_decimals: 18
    wrapped_native_address: "0x0d500B1d8E8eF31E21C99d1Db9A6444d3ADf1270"
    # Базовый токен круга этой сети: вход и выход цикла.
    base_token_address: "0xc2132D05D31c914a87C6611C10748AEb04B58e8F"
    rpc_url: "https://polygon-bor-rpc.publicnode.com"
    # Запасные узлы. Публичные узлы отказывают — 18.09 основной узел
    # Arbitrum начал отвечать 403 на всё подряд, и подобрать квитанцию
    # уже отправленной покупки стало нечем. Проверены живыми запросами
    # в тот же день.
    rpc_fallback_urls:
      - "https://polygon.drpc.org"
    # Значок сети в уведомлении Telegram.
    emoji: "⛓️"
    enabled: true
    # Надбавка к базовой цене газа. В Polygon за место в блоке идёт торг,
    # и сеть требует не меньше 25 gwei: без надбавки транзакция может не
    # попасть в блок вовсе. Значение то же, что действовало до вынесения
    # надбавки в настройку сети.
    priority_fee_wei: 30000000000

  - network_id: arbitrum
    name: Arbitrum
    chain_id: 42161
    native_token_symbol: ETH
    native_token_decimals: 18
    wrapped_native_address: "0x82aF49447D8a07e3bd95BD0d56f35241523fBab1"
    # USDT на Arbitrum. Адрес другой, чем на Polygon: одинаковый символ
    # не означает одинаковый контракт (01_PROJECT_REQUIREMENTS.md §11).
    base_token_address: "0xFd086bC7CD5C481DCC9C85ebE478A1C0b69FCbb9"
    # Официальный узел сети. Прежний (publicnode) 18.09 начал отвечать
    # отказом 403 на все запросы подряд — вплоть до того, что подобрать
    # квитанцию уже отправленной покупки стало нечем.
    rpc_url: "https://arb1.arbitrum.io/rpc"
    rpc_fallback_urls:
      - "https://arbitrum.drpc.org"
      - "https://arbitrum-one.public.blastapi.io"
    emoji: "🪀"
    enabled: true
    # В Arbitrum торга за место в блоке нет: проверка живых блоков 18.09
    # показала, что все транзакции просят надбавку ноль и платят ровно
    # базовую цену — около 0,02 gwei. Надбавка в 30 gwei, действовавшая
    # для Polygon, удорожила бы здесь каждую транзакцию в полторы тысячи
    # раз.
    priority_fee_wei: 0

# Провайдер включается только вместе с заданной переменной окружения
# для его ключа: enabled-провайдер без доступного секрета останавливает старт.
providers:
  - provider_id: oneinch
    enabled: false
    emoji: "🥏"
    ui_url: "https://app.1inch.io"
    supported_networks: [polygon]
    request_timeout_seconds: 10.0
    max_concurrent_requests: 1
    requests_per_second: 0.9

  - provider_id: zero_x
    emoji: "🔫"
    ui_url: "https://matcha.xyz"
    # Отключён: лимит запросов бесплатного плана исчерпан. Проверка
    # 14.09 показала, что он не восстановился — API снова ответил 403.
    # Возврат — по решению оператора, одной строкой.
    enabled: false
    # Покупка и продажа у этого агрегатора. Разрешение двухуровневое:
    # глобальное routes.allow_same_provider плюс согласие провайдера.
    allow_same_provider_round_trip: true
    api_key: { env: "MONIK_ZEROX_API_KEY" }
    requests_per_second: 4.9
    # 0x чаще остальных отвечает ошибкой частоты: за два часа наблюдения
    # 15 повторов против нуля у Velora и Uniswap. Пауза только для него,
    # общая остаётся 0.2 (resources.provider_min_interval_seconds).
    min_interval_seconds: 0.3
    # Часы работы. Вне окна агрегатор не опрашивается вовсе.
    schedule:
      start: "04:00"
      end: "23:00"
    supported_networks: [polygon]

  - provider_id: velora
    enabled: true
    emoji: "👾"
    ui_url: "https://app.velora.xyz"
    # Покупка и продажа у этого агрегатора. Разрешение двухуровневое:
    # глобальное routes.allow_same_provider плюс согласие провайдера.
    allow_same_provider_round_trip: true
    api_key: { env: "MONIK_VELORA_API_KEY" }
    # Документированный бесплатный тариф Velora — 1 запрос в секунду и
    # 5000 в сутки (velora.xyz/docs/integrate/rate-limits). Здесь по
    # решению оператора выставлено 4: суточный расход удерживает окно
    # работы, а частота проверяется опытом. При отказах 429 предохранитель
    # разведёт пробы по лестнице охлаждения.
    requests_per_second: 4.0
    # Пауза согласована с частотой: более строгое из двух ограничений и
    # определяет темп, поэтому прежняя секундная пауза свела бы настройку
    # частоты к одному запросу в секунду.
    min_interval_seconds: 0.25
    # Часы работы. Короткое окно бережёт суточную квоту бесплатного
    # тарифа: за 12 часов расход вдвое меньше, чем за сутки.
    schedule:
      start: "07:00"
      end: "19:00"
    # Только основной режим: при интервале fest в 30 секунд Velora одна
    # стоила бы 8640 запросов за окно при квоте 5000 в сутки.
    modes: [ur]
    # Только Polygon. API Velora работает и на Arbitrum, но суточная
    # квота бесплатного тарифа — 5000 запросов, а один Polygon уже
    # расходует около 44 за цикл. Вторая сеть удвоила бы расход без
    # запаса. Включение — одной строкой, когда появится платный тариф.
    supported_networks: [polygon]

  - provider_id: kyberswap
    # Выключен по решению оператора 18.09. Исполнение сделок его адаптер
    # не поддерживает: он умеет только получать котировки.
    enabled: false
    # Участвует в обоих режимах.
    modes: [ur, fest]
    emoji: "🖲️"
    ui_url: "https://kyberswap.com/swap/"
    # Покупка и продажа у этого агрегатора. Разрешение двухуровневое:
    # глобальное routes.allow_same_provider плюс согласие провайдера.
    allow_same_provider_round_trip: true
    # Ключ доступа API не требует. Значение передаётся заголовком
    # x-client-id, которым приложение называет себя; на цену и на лимит
    # частоты оно не влияет — как X-Partner у Velora.
    api_key: { env: "MONIK_KYBERSWAP_API_KEY" }
    # Публичный лимит — 3 запроса в секунду, суточной квоты нет
    # (docs.kyberswap.com, раздел rate limits). Проба с частотой 4 дала 13
    # отказов по лимиту за 23 минуты, поэтому темп возвращён в рамки.
    requests_per_second: 3.0
    min_interval_seconds: 0.35
    # Часы работы. Вне окна агрегатор не опрашивается вовсе.
    schedule:
      start: "04:00"
      end: "23:00"
    supported_networks: [polygon, arbitrum]
    # Сеть зашита в адрес страницы обмена, поэтому общая ссылка привела
    # бы оператора не в ту сеть.
    ui_urls:
      polygon: "https://kyberswap.com/swap/polygon"
      arbitrum: "https://kyberswap.com/swap/arbitrum"

  - provider_id: uniswap
    enabled: true
    # Единственный агрегатор торгового режима: на суммах 50-100 USDT
    # только у него газ укладывается в целевую прибыль.
    modes: [ur, fest, ann]
    emoji: "🧩"
    ui_url: "https://app.uniswap.org/swap"
    # Покупка и продажа у этого агрегатора. Разрешение двухуровневое:
    # глобальное routes.allow_same_provider плюс согласие провайдера.
    allow_same_provider_round_trip: true
    api_key: { env: "MONIK_UNISWAP_API_KEY" }
    requests_per_second: 4.9
    # Часы работы. Вне окна агрегатор не опрашивается вовсе.
    schedule:
      start: "04:00"
      end: "23:00"
    supported_networks: [polygon, arbitrum]
    ui_urls:
      polygon: "https://app.uniswap.org/swap?chain=polygon"
      arbitrum: "https://app.uniswap.org/swap?chain=arbitrum"
    options:
      # Адрес, от имени которого строится котировка и собирается
      # транзакция. Это торговый счёт режима ann: транзакцию исполнит
      # именно он, и котировка должна быть посчитана для него же.
      swapper: "0xc6fb9692Fff871e1b4874E8Dc0620fd77CE02390"

# Token universe. Authoritative metadata живёт в Token Registry, здесь
# задаётся разрешённый набор. decimals указывается явно и не выводится
# из символа (01_PROJECT_REQUIREMENTS.md §10).
tokens:
  - network_id: polygon
    address: "0xc2132D05D31c914a87C6611C10748AEb04B58e8F"
    symbol: USDT
    decimals: 6
    usd_stable: true
    rank: 1

  - network_id: polygon
    address: "0x3c499c542cEF5E3811e1192ce70d8cC03d5c3359"
    symbol: USDC
    decimals: 6
    usd_stable: true
    rank: 2

  - network_id: polygon
    address: "0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174"
    symbol: USDC.e
    decimals: 6
    rank: 3
    # Мостовой USDC на Polygon — такой же долларовый стейблкоин, как USDC.
    usd_stable: true

  - network_id: polygon
    address: "0x7ceB23fD6bC0adD59E62ac25578270cFf1b9f619"
    symbol: WETH
    decimals: 18
    rank: 4

  - network_id: polygon
    address: "0x0d500B1d8E8eF31E21C99d1Db9A6444d3ADf1270"
    symbol: WPOL
    decimals: 18
    rank: 5

  - network_id: polygon
    address: "0x1BFD67037B42Cf73acF2047067bd4F2C47D9BfD6"
    symbol: WBTC
    decimals: 8
    rank: 6

  - network_id: polygon
    address: "0x8f3Cf7ad23Cd3CaDbD9735AFf958023239c6A063"
    symbol: DAI
    decimals: 18
    usd_stable: true
    rank: 7

  - network_id: polygon
    address: "0xD6DF932A45C0f255f85145f286eA0b292B21C90B"
    symbol: AAVE
    decimals: 18
    rank: 8

  - network_id: polygon
    address: "0x53E0bca35eC356BD5ddDFebbD1Fc0fD03FaBad39"
    symbol: LINK
    decimals: 18
    rank: 9

  - network_id: polygon
    address: "0xb33EaAd8d922B1083446DC23f610c2567fB5180f"
    symbol: UNI
    decimals: 18
    rank: 10

  - network_id: polygon
    address: "0xc3c7d422809852031b44ab29eec9f1eff2a58756"
    symbol: LDO
    decimals: 18
    rank: 13

  - network_id: polygon
    address: "0x03b54A6e9a984069379fae1a4fC4dBAE93B3bCCD"
    symbol: wstETH
    decimals: 18
    rank: 14

  - network_id: polygon
    address: "0xdf7837de1f2fa4631d716cf2502f8b230f1dcc32"
    symbol: TEL
    decimals: 2
    rank: 27

  - network_id: polygon
    address: "0x3a58a54c066fdc0f2d55fc9c89f0415c92ebf3c4"
    symbol: stMATIC
    decimals: 18
    rank: 29

  # Добавлены 15.09 по измерению стоимости круга на 1000 USDT: у всех
  # трёх проход через пулы дешевле 0,7 %, что оставляет место расхождению
  # цен между агрегаторами.

  - network_id: polygon
    address: "0xfa68FB4628DFF1028CFEc22b4162FCcd0d45efb6"
    symbol: MaticX
    decimals: 18
    rank: 31

  - network_id: polygon
    # Евро-стейблкоин: относительно USDT он подвижен, поэтому меткой
    # usd_stable не помечается — интересен именно расхождением EUR/USD.
    address: "0xE0aEa583266584DafBB3f9C3211d5588c73fEa8d"
    symbol: EURe
    decimals: 18
    rank: 32

  - network_id: polygon
    # Мостовой SOL (Wormhole). Знаков 9, а не 18: значение берётся из
    # реестра токенов и из символа не выводится.
    address: "0xd93f7E271cB87c23AaA73edC008A79646d1F9912"
    symbol: SOL
    decimals: 9
    rank: 33

  # --- Arbitrum -------------------------------------------------------
  #
  # Набор собран по замеру круга USDT → X → USDT 15.09 на суммах от 1000
  # до 10000 USDT. Отличие сети от Polygon в том, что у мажоров стоимость
  # круга не растёт вместе с суммой: WETH на 10000 стоит 0,027 % против
  # 0,066 % на Polygon, WBTC — 0,035 % против 0,102 %. Адреса и знаки
  # сверены вызовом decimals()/symbol() у контрактов сети.

  - network_id: arbitrum
    address: "0xFd086bC7CD5C481DCC9C85ebE478A1C0b69FCbb9"
    symbol: USDT
    decimals: 6
    usd_stable: true
    rank: 1

  - network_id: arbitrum
    address: "0xaf88d065e77c8cC2239327C5EDb3A432268e5831"
    symbol: USDC
    decimals: 6
    usd_stable: true
    rank: 2

  - network_id: arbitrum
    # Мостовой USDC. Контракт в сети отвечает символом USDC — это
    # косметическое расхождение: идентичность задаёт адрес.
    address: "0xFF970A61A04b1cA14834A43f5dE4533eBDDB5CC8"
    symbol: USDC.e
    decimals: 6
    usd_stable: true
    rank: 3

  - network_id: arbitrum
    address: "0xDA10009cBd5D07dd0CeCc66161FC93D7c9000da1"
    symbol: DAI
    decimals: 18
    usd_stable: true
    rank: 4

  - network_id: arbitrum
    address: "0x82aF49447D8a07e3bd95BD0d56f35241523fBab1"
    symbol: WETH
    decimals: 18
    rank: 5

  - network_id: arbitrum
    address: "0x2f2a2543B76A4166549F7aaB2e75Bef0aefC5B0f"
    symbol: WBTC
    decimals: 8
    rank: 6

  - network_id: arbitrum
    address: "0x912CE59144191C1204E64559FE8253a0e49E6548"
    symbol: ARB
    decimals: 18
    rank: 7

  - network_id: arbitrum
    address: "0xf97f4df75117a78c1A5a0DBb814Af92458539FB4"
    symbol: LINK
    decimals: 18
    rank: 8

routes:
  # Покупка и продажа у одного агрегатора. Архитектура это допускает при
  # явном разрешении (02_LEVEL1_SCANNER.md §18); основной моделью остаётся
  # кросс-провайдерный арбитраж. Включено для наблюдения: направление
  # почти наверняка убыточно — вход и выход идут по одной кривой
  # ликвидности, — но проверяется это замером, а не рассуждением.
  allow_same_provider: true
  allowed_pairs: []
  max_route_steps: 4

scanner:
  # Сканируемые сети и базовый токен каждой из них заданы в разделе
  # networks: сканируются все включённые сети, каждая своим циклом.
  # Суммы задаются только конфигурацией (01_PROJECT_REQUIREMENTS.md §22).
  # Суммы, которыми Level 2 проверяет найденную возможность. Их число
  # произвольно и на стоимость поиска не влияет.
  amounts: ["50", "100", "300", "500"]

  # Режимы сканирования. Каждый — именованный проход Level 1 со своим
  # темпом и своей планкой (the_main_rules.md, правило 10). Уровни при
  # этом не меняются: Level 1 ищет, Level 2 подтверждает.
  modes:
    # ur — основной проход: весь набор токенов, все включённые
    # агрегаторы. Сейчас выключен по решению оператора.
    ur:
      enabled: false
      interval_seconds: 300
    # fest — частый проход по стейблкоинам. Набор определяется меткой
    # usd_stable, поэтому EURe в него не входит: он привязан к евро и
    # интересен расхождением EUR/USD, а не отклонением от паритета.
    # Состав агрегаторов задан у них самих (providers[].modes).
    fest:
      # Остановлен по решению оператора 17.09.
      enabled: false
      interval_seconds: 30
    # ann — торговый проход (the_main_rules.md, правило 11). Сканирует все
    # суммы сразу, выбирает лучшую находку по заработку в USDT и исполняет
    # сделку сам. Пока подсистемы исполнения нет, его находки только
    # записываются в журнал — это сухой прогон режима.
    ann:
      enabled: true
      # Десять секунд по решению оператора 17.09. Отклонение между
      # стейблкоинами живёт минуты, и редкий проход его не застаёт.
      interval_seconds: 10
      # Свой срок прохода: общий (25 с) в такой интервал не укладывается,
      # а понижать его для всех нельзя — медленный обход всего набора
      # токенов в режиме ur столько не успевает.
      scan_timeout_seconds: 10
      # Свои суммы: торговый проход ограничен остатком счёта, тогда как
      # суммы проверки Level 2 — вопрос анализа и от денег на кошельке
      # не зависят. Заданы оператором 17.09.
      amounts: ["50", "100"]
      # Сети режима перечислены обе; работающая отмечена true. Включение
      # подготовленной сети — правка одного слова.
      #
      # 18.09 по решению оператора торговый проход переведён в Arbitrum.
      # Polygon остаётся объявленным и включается обратно одним словом.
      networks:
        polygon: false
        arbitrum: true

  level1:
    # Сумма поиска. Level 1 работает одной суммой: остальные Level 2
    # подставляет в уже найденную возможность.
    amount: "50"
    overlap_policy: skip
    # Таймаут прохода. Не может превышать интервал самого частого
    # включённого режима, иначе проходы накладывались бы по построению.
    scan_timeout_seconds: 25
    top_tokens: 30
    max_opportunities_per_scan: 50
    max_concurrent_requests: 8
    quote_max_age_seconds: 30
    # Срок, в течение которого возможность можно проверять. Поднят до
    # десяти минут, чтобы проверка пережила ручную остановку сканера: при
    # восстановлении просроченное задание отменяется, а не начинается
    # заново. Возраст возможности при этом считается по календарю —
    # время простоя из него не вычитается.
    opportunity_ttl_seconds: 600
    deduplication_window_seconds: 300

  level2:
    enabled: true
    max_parallel: 20              # CLAUDE.md §18
    queue_capacity: 200
    # Срок жизни задания Level 2. Совпадает со сроком возможности: если
    # возможность истечёт раньше, задание останется без предмета проверки.
    job_ttl_seconds: 600
    confirmation_timeout_seconds: 60
    max_attempts: 3
    quote_max_age_seconds: 15

profitability:
  threshold_metric: net_roi
  # Порог каждого режима. Один на оба уровня: Level 1 ищет и Level 2
  # подтверждает по одной планке (the_main_rules.md, правило 10).
  # Сравнение >=, ровно указанное значение проходит.
  thresholds:
    ur: "0.1"
    fest: "0.02"
    ann: "0.01"

# Исполнение сделок режима ann. Отдельная подсистема со своим
# выключателем (the_main_rules.md, правило 11): режим может искать, не
# торгуя, — это сухой прогон.
trading:
  # Пока выключено: сделки не отправляются, находки только пишутся в
  # журнал. Ключ при этом читается — он нужен, чтобы видеть балансы.
  execution_enabled: true
  private_key: { env: "MONIK_TRADING_PRIVATE_KEY" }
  # Допуск проскальзывания. Это защита от завышенной котировки: если
  # реальный пул не даёт обещанного, сделка откатывается сетью, и мы
  # теряем газ вместо исполнения по плохому курсу.
  slippage_percent: "0.1"
  # Минимальная чистая прибыль для закрытия позиции, в базовом токене.
  # Чистая — за вычетом газа обеих ног: газ от суммы сделки не зависит и
  # на малых суммах решает исход.
  #
  # Первый порог действует на проверке сразу после покупки, второй — у
  # сделки, ушедшей в ожидание: деньги уже заперты в позиции, и выйти из
  # них выгоднее, чем ждать прежней прибыли. Заданы оператором 17.09.
  min_exit_profit: "0.01"
  min_exit_profit_waiting: "0.005"
  recheck_interval_seconds: 10
  receipt_timeout_seconds: 120
  receipt_poll_seconds: 2
  # Предельный срок ожидания выхода. По его истечении приходит
  # уведомление; позицию в убыток система не закрывает никогда.
  max_wait_notice_enabled: true
  max_wait_notice_seconds: 7200
  approve_unlimited: true
  report_interval_seconds: 1800

fees:
  enabled: true
  refresh_on_startup: true
  refresh_interval_days: 1
  refresh_time: "02:00"
  freshness_seconds: 86400
  batch_enabled: true

# Gas и цены подключаются независимыми реализациями Provider (решение D-4).
gas:
  enabled: true
  # adapter_estimate — gas units из маршрута; rpc — цена газа из узла сети.
  # static используется только как явно настроенный fallback.
  # Порядок источников цены газа. quote — цена, пришедшая вместе с
  # котировкой агрегатора: лишнего запроса не требует, потому что
  # значение уже получено. rpc остаётся запасным на случай провайдера,
  # который цену не сообщает. adapter_estimate — расход газа из маршрута.
  sources: [quote, adapter_estimate, rpc]
  freshness_seconds: 60
  request_timeout_seconds: 5.0
  # static_wei_per_gas:
  #   polygon: 120000000000

  # Поправка к оценке расхода газа из котировки. Агрегатор оценивает голый
  # обмен по одному лучшему пути, а платим мы за реальный вызов роутера —
  # с разрешениями, обёртками и маршрутом, который на исполнении может
  # разойтись на несколько пулов.
  #
  # Значение измерено 18.09 в обеих сетях одинаково: стоимость круга по
  # пределам газа собранных транзакций против суммы долларовых оценок из
  # котировок дала 1,312 в Polygon и 1,294 в Arbitrum. Взято 1,3.
  #
  # Фактический расход исполненной сделки бывает заметно выше: на #T1
  # маршрут продажи разошёлся на два пула, и вышло втрое против оценки.
  # Такие случаи ловит не этот множитель, а точная проверка стоимости
  # перед самой сделкой — она считает по пределам газа уже собранных
  # транзакций обеих ног.
  quote_estimate_multiplier: "1.0"
  quote_estimate_multipliers:
    uniswap: "1.3"

  # Самокалибровка: обещанное приходит с котировкой, фактическое — с
  # квитанцией, которую подсистема исполнения запрашивает в любом случае.
  # Поправка поэтому не стоит ни одного дополнительного обращения и сама
  # следует за изменением маршрутов.
  calibration:
    enabled: true
    # Одна сделка — не статистика: до пяти замеров действует значение выше.
    min_samples: 5
    # Предел накопленной истории. По его достижении накопленное
    # уполовинивается, иначе давние замеры навсегда перевешивали бы свежие.
    max_samples: 100

prices:
  enabled: true
  sources: [aggregator_quote]
  freshness_seconds: 300
  request_timeout_seconds: 5.0

resources:
  global_max_concurrent_requests: 32
  provider_min_interval_seconds: 0.2
  queue_capacity: 1000
  queue_wait_timeout_seconds: 30.0
  default_request_timeout_seconds: 10.0
  lease_timeout_seconds: 60.0
  deduplicate_in_flight: true
  retry:
    max_attempts: 3               # CLAUDE.md §32
    initial_delay_seconds: 0.5
    max_delay_seconds: 30.0
    backoff_multiplier: 2.0
    jitter_ratio: 0.2
  circuit_breaker:
    enabled: true
    failure_threshold: 5
    recovery_timeout_seconds: 30.0
    half_open_max_calls: 1
    success_threshold: 2

scheduler:
  enabled: true
  tasks:
    # По задаче на режим. Период каждой берётся из scanner.modes и здесь
    # не дублируется: параметром управляют в одном месте. Указанное здесь
    # значение, расходящееся с настройкой режима, остановит старт.
    scan_ur:
      mode: interval
      overlap_policy: skip
    scan_fest:
      mode: interval
      overlap_policy: skip
    fee_refresh:
      mode: daily
      time: "02:00"
      timezone: "Europe/Lisbon"
      interval_days: 1
    capability_refresh:
      mode: startup
    # Напоминание о доступных, но не установленных обновлениях. Ничего
    # не устанавливает: автоматически ставятся только обновления
    # безопасности, остальные ждут решения оператора.
    system_updates:
      mode: daily
      time: "09:00"
      timezone: "Europe/Lisbon"
      interval_days: 1
    cleanup:
      mode: daily
      time: "03:30"
      timezone: "Europe/Lisbon"
      interval_days: 1

health:
  enabled: true
  check_interval_seconds: 60
  provider_degraded_threshold: 2     # порог перехода в DEGRADED
  provider_failure_threshold: 4      # порог перехода в UNAVAILABLE
  provider_recovery_threshold: 2     # гистерезис: порог восстановления
  worker_restart_limit: 3

notifications:
  enabled: true
  mode: A                         # A | B — влияет только на правила отправки
  language: ru
  decimal_places: 6
  queue_capacity: 500
  max_attempts: 5
  retry_initial_delay_seconds: 2.0
  retry_max_delay_seconds: 300.0
  deduplication_window_seconds: 3600
  show_calculation_version: false  # версия формулы в тексте уведомления
  # Значки уведомления о возможности. Значок агрегатора задан у самого
  # агрегатора, значок сети — у сети: свойство живёт там, где его владелец.
  emoji:
    pair: "💱"        # строка направления обмена
    best_roi: "⭐"     # сумма с наибольшим процентом доходности
    best_profit: "💎"  # сумма с наибольшей прибылью в базовом токене
    partial: "⚠️"      # сумма, не получившая подтверждения
    time: "⌚"        # время завершения проверки Level 2
  # Режим влияет ТОЛЬКО на правила отправки и не меняет Level 1 / Level 2.
  mode_a:
    send_confirmed: true
    send_partial: true
  mode_b:
    send_confirmed: true
    send_partial: true
  telegram:
    enabled: true
    bot_token: { env: "MONIK_TELEGRAM_BOT_TOKEN" }
    chat_id: { env: "MONIK_TELEGRAM_CHAT_ID" }
    api_base_url: "https://api.telegram.org"
    request_timeout_seconds: 10.0
    commands_enabled: true
    poll_interval_seconds: 2.0

database:
  # База вынесена за пределы каталога состояния: переход на следующую
  # версию не должен затрагивать историю. Каталог создаётся один раз и
  # принадлежит пользователю службы.
  path: "/var/lib/monik/monik.db"
  wal_enabled: true
  busy_timeout_seconds: 5.0
  integrity_check_on_startup: true
  cleanup_enabled: true
  retention:
    opportunities_days: 90
    jobs_days: 30
    notifications_days: 30
    scans_days: 14
    fee_snapshots_days: 30

logging:
  level: INFO
  directory: logs
  retention_days: 14

metrics:
  enabled: true
  collection_interval_seconds: 60
